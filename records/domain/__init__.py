"""The school's vocabulary, with nothing that does I/O.

Grading policy, terms, marks, the children a guardian may hear about, and the ways this
facade refuses. Nothing here imports FastAPI, `httpx`, or `records.config` — which is what
makes "does an excused assignment leave the denominator" a function call rather than a
request.

The hexagon's centre. `ports/` states what it needs from outside; `adapters/` supplies it;
`application/` orchestrates. Nothing in this package knows any of them exist.
"""
