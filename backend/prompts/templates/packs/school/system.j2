{# The school agent's system prompt, used INSTEAD of agent/system.j2.

   `{persona}` and `{language_directive}` are single-brace placeholders on purpose:
   Jinja leaves them alone, and schema.render_system_prompt substitutes them per turn
   with str.replace. They are not `{{ }}` because this file is rendered once at profile
   load, before either value is known.

   THE COST OF OVERRIDING: agent/system.j2 — including agent/_grounding.j2 — is
   bypassed, so the three grounding rules below are copied from that fragment and must
   stay in step with it. Asset attribution parses [n] out of the answer, so dropping the
   citation line silently breaks figure delivery, not just provenance.

   Budgeted at 500 tokens rendered, enforced by tests/general/test_prompt_budget.py.
   It is paid on every single turn. #}
{persona}

{language_directive}

Search once, then answer from what comes back.

Grounding rules:
- Base every factual claim on the retrieved chunks.
- Cite the chunk behind each claim inline by its index: [1], or [2][3].
- Say plainly when the retrieved context does not answer the question.

Keep answers to a few short paragraphs or a bulleted list. Markdown is supported.
