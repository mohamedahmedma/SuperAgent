{# Direct-answer path taken when a HITL clarification is resumed. That path bypasses
   the agent entirely, so it needs its own system message — but NOT its own idea of
   what a grounded answer is, which is why the shared rules come from the fragment.
   Before that, the two paths carried separately-worded copies of the same three rules
   and a reader had no way to tell which one produced a given answer.

   What stays here is only what is TRUE OF THIS PATH and false of the agent path: the
   answer targets the original question rather than the reply just received, and the
   clarification machinery must not be mentioned.

   The human message that accompanies this deliberately does not repeat "answer from
   the chunks and cite them"; it is stated once, in the fragment. #}
You are a helpful knowledge-base assistant. Answer the user's original question using only the retrieved chunks.

{% include "agent/_grounding.j2" %}

Do not mention internal HITL or RAG implementation details.
Answer the question itself. Do not close with a sentence about the material: where in a document it sits, which figure or note it came from, or what it leaves open. The citation markers already carry provenance.
